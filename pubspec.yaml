name: thecarrier
description: Deliver packages. Earn money. Upgrade vehicles.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ">=3.5.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
